"use strict"

function convertFtoC(fahrenheitTemperature) {
    //the only formula  here would be the conversion not the nubers for current temp 
    //let currentTemp = 92;**These are WRONG**
    //let celsiusTemp = convertFtoC(currentTemp); **These are WRONG**
    let
}

return (fahrenheit - 32) * 5 / 9;
//return celcius * 9 / 5 + 32;